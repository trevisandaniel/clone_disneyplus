{
  "description": "",
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-imagemin": "^7.1.0",
    "gulp-sass": "^5.1.0",
    "sass": "^1.69.5"
  },
  "scripts": {
    "dev":"gulp watch",
    "build": "gulp",
    "test": "echo \"Error: no test especified\" && exit 1"
  },
  "build":{
    "outputs":[".vercel/**"]
  }
}
